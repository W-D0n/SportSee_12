import styled from 'styled-components';

const Div = styled.div`
color: white;
`
const PerformanceCard = () => {
//  titre +background + img(option) + un graph + une data
<Div>
  
</Div>
}

export default PerformanceCard;